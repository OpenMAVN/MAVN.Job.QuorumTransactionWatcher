apiVersion: v1
kind: Service
metadata:
  labels:
    app: quorumtransactionwatcher
    environment: Development
  name: quorumtransactionwatcher
  namespace: jobs
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: quorumtransactionwatcher
  sessionAffinity: None
  type: ClusterIP
